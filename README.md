# Batch-Data-Pipeline
Built an automated end-to-end batch data pipeline that ingests raw data into database, performs cleaning and transformations, then loads analytics-ready tables into a data warehouse, and serves business KPIs through dashboards.
